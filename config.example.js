// Copy this file to `config.js` and fill in the values from your Supabase project.
// (Settings → API in the Supabase dashboard.)
//
// The anon key is designed to be public — security comes from the RLS policy
// in supabase-schema.sql, which only allows INSERT into `waitlist`.
window.RECUTER_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-KEY"
};
